import { CambioVocalesPipe } from './cambio-vocales.pipe';

describe('CambioVocalesPipe', () => {
  it('create an instance', () => {
    const pipe = new CambioVocalesPipe();
    expect(pipe).toBeTruthy();
  });
});
