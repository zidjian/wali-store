import { HaceCuantoPipe } from './hace-cuanto.pipe';

describe('HaceCuantoPipe', () => {
  it('create an instance', () => {
    const pipe = new HaceCuantoPipe();
    expect(pipe).toBeTruthy();
  });
});
