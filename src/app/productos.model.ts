export interface Producto { // exportamos la interfaz de Producto
    nombre: string, // Valor requerido
    precio: number, // Valor requerido
    imagen: string, // Valor requerido
    descripcion?: string // Valor opcional
}
